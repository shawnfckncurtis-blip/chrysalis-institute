## **Executive Summary of the Resolution**

The tension between the two modeling methodologies has been mathematically resolved. By introducing an explicit bilinear composite $M\_{ab}^{s\_1s\_2}(\\hat{q},\\hat{p})$ at the flat-slice kinematic limit (p̂ \= \-q̂, k → 0), the open split between the conventions has collapsed into definitive behaviors:

> * **Convention II (Raw Symmetrized Product)** exactly recovers the published isotropic tensor coefficients ($A=\\frac{1}{5}, B=\\frac{1}{30}$). However, it leaves a non-vanishing trace on internal index pairs ($M^a{}\_a \= 1$), meaning it carries a raw helicity-0 configuration and cannot be structurally classified as a transverse-traceless "Cotton composite."  
> * **Convention I (Internal Transverse-Traceless)** applies the transverse-traceless projector $S\_\\perp$ *pointwise* prior to the angular average. Because the explicit tensor $M\_{ab}^{+-}$ lies entirely parallel to the transverse planar projector ($M\_{ab}^{+-} \= \\frac{1}{2}P\_{ab}$), its internal trace completely balances its transverse components, causing the projection $(S\_\\perp M)\_{ab}$ to **vanish identically**.

Consequently, under Convention I, the tensor vanishes ($A\_{\\text{TT}} \= B\_{\\text{TT}} \= 0$), revealing that the previously published external contact factor ($2B \= \\frac{1}{15}$) is an unprotected artifact of the raw helicity-0 sector rather than a robust geometric property of a projected tensor.

## ---

**Comparative Evaluation Matrix**

The structural behaviors and algebraic realities of both paradigms are detailed below:

| Feature / Metric | Convention I (Cotton / Internal TT) | Convention II (Raw Symmetrized Product) |
| :---- | :---- | :---- |
| **Pointwise Internal Trace ($M^a{}\_a$)** | Must vanish; fails prior to projection ($M^a{}\_a \= 1$). | Permitted to be non-zero (= 1 for opposite-helicity). |
| **Action of Internal $S\_\\perp$** | Annihilates the composite ($\\mathscr{E}\_{abcd} \= 0$). | Omitted / Not applied. |
| **Isotropic Coefficients (A, B)** | (0, 0\) | $\\left(\\frac{1}{5}, \\frac{1}{30}\\right)$ |
| **External TT Contact (2B)** | 0 | $\\frac{1}{15}$ |
| **Counterterm Requirements** | No leading isotropic contact from this block. | Trace sector required; standard $C\_\\Sigma^2$ is insufficient. |
| **Permitted Nomenclature** | "Cotton composite" | **Must not** be designated as Cotton on internal indices. |
| **Isotropic q¹² Core Status** | **Vanishes identically** | **Survives** |

## ---

**Key Validated Algebraic Realities**

The rigorous structural checks from the preceding algebraic analysis hold true:

> 1. **Pointwise Constraint Application:** The internal transverse-traceless constraint must be evaluated pointwise on the individual helicity states $M\_{ab}^{s\_1s\_2}$ before taking the spherical average, rather than treated as a post-integration smoothing parameter.  
> 2. **Subspace Dimensionality:** For a structurally valid, internally TT composite, the rotationally averaged rank-4 tensor contains only **one** independent scalar normalization degree of freedom (λ), governed by the constraint 3A \+ 2B \= 0 ($A \= \-\\frac{2}{3}B$). The published pair violates this restriction.  
> 3. **Double Trace Diagnostic:** The double trace $\\langle \\mathscr{E}^{ab}{}\_{ab} \\rangle\_\\Omega \= 3A \+ 12B \= 1$ confirms a raw, unprojected normalization scheme matching Convention II.

## ---

**Required Updates to the Ledger**

The technical records require the following updates to ensure formal consistency:

> * **Isotropic Coefficients:** Restrict $A \= \\frac{1}{5}$ and $B \= \\frac{1}{30}$ exclusively to **Convention II** notations, explicitly appending the unprojected bilinear form.  
> * **Contact Normalization:** Flag the $\\frac{1}{15}$ external contact factor as **Convention II only**.  
> * **Convention I Entry:** Add an exact zero-result branch (A=0, B=0, Contact \= 0\) for the internal-TT projection under identical kinematics.  
> * **Physical Remainder Gate:** Retain the security gate on the radial ultraviolet degree, finite-k kernel, and counterterm dependencies. No physical transfer claim can be advanced without isolating a finite, regulator-independent, non-local remainder that successfully survives all field, counterterm, and state redefinitions.

For the **next legitimate step** focusing on the finite-k kernel where $\\hat{p} \= \\widehat{\\mathbf{k}-\\mathbf{q}}$:

> * Which **single convention** (I or II) should be isolated for the mode normalization?  
> * Are there specific **counterterm matching criteria** or regularizers preferred for handling the trace sector if Convention II is pursued?
